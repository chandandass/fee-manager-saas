// Deprecated: OAuth uses src/app/auth/callback/page.tsx (client) so hash tokens work.
// This file intentionally does not export handlers to avoid conflicting with page.tsx.
// Delete this file locally if Next.js complains about route conflict.
export {};
